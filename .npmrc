auto-install-peers=false
strict-peer-dependencies=false
# Don't re-run the dependency-status install check before every `pnpm <script>`.
# Avoids `pnpm dev` aborting on benign states (e.g. a not-yet-approved build script).
verify-deps-before-run=false
